# `mgrep` 

mgrep.md

*   CLI-native way to semantically grep everything

*   cons

    *   API key needed

    *   indexed on server
    
*   semantic search for everything

*   https://github.com/mixedbread-ai/mgrep

```shell
npm install -g @mixedbread/mgrep    # or pnpm / bun
```

*   https://elite-ai-assisted-coding.dev/p/mgrep-with-founding-engineer-rui

mgrep for agents:

    Claude Code

    ```
    mgrep install-claude-code
    ```

    OpenCode

    ```
    mgrep install-opencode
    ```
    
    Codex

    ```
    mgrep install-codex
    ```
