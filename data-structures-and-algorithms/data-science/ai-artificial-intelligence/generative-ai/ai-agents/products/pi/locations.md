# Locations (Files, Configs)

locations.md

*   https://pi.dev/docs/latest/skills#locations

*   global

    ```
    $HOME/.pi/agent/skills/
    ```

    ```
    $HOME/.agents/skills/
    ```

*   project

    ```
    .pi/skills/
    ```

    ```
    .agents/skills/
    ```

    *   in cwd and ancestor directories

        *   up to git repo root

        *   filesystem root when not in a repo