# Editor + Ollama

editors-with-ollama.md

*  VSCode + Extension: Continue

*  Rider +  Extension: Continue


*   Continue

    *   File Locations:

        *   macOS/Linux: 
    
            ```shell
            $HOME/.continue/config.yaml
            ```


        *   Windows 
        
            ```shell
            %USERPROFILE%\.continue\config.yaml
            ```


        *   Models

            ```shell
            $HOME/.continue/models
            ```

        *   Rules

            ```shell
            $HOME/.continue/rules
            ```

        *   Tools

            ```shell
            $HOME/.continue/mcpServers
            ```


    *   Global

        *   `.env` file 

            ```shell
            ~/.continue/.env
            ```

    *   Workspace

        *   `.env` file 
        
            *   project root

*   https://docs.continue.dev/guides/configuring-models-rules-tools

