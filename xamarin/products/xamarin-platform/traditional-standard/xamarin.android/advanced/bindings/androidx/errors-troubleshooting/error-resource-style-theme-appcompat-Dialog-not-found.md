# AndroidX `resource style/Theme.AppCompat.Dialog not found`

error-resource-style-theme-appcompat-Dialog-not-found.md

Error:

```
	resource style/Theme.AppCompat.Dialog not found.

```

Add package `Xamarin.AndroidX.AppCompat`
